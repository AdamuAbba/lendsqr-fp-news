import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  googleLottieView: {
    height: 45,
  },
  buttonTouchableView: {
    marginBottom: 40,
    width: 200,
    elevation: 5,
  },
  innerButtonView: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  buttonText: {
    marginLeft: 40,
  },
});

export {styles};

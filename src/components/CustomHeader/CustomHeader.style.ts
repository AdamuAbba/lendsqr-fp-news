import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  signOutBtn: {
    flexDirection: 'row',
    marginRight: 20,
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export {styles};

import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },

  welcomeText: {
    marginBottom: 20,
  },
  paragraph: { marginBottom: 20, textAlign: 'center', width: 300 },
});

export { styles };

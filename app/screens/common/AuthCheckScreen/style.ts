import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
    height: '100%',
    width: '100%',
    flex: 1,
    backgroundColor: '#fff',
  },
  lottieStyle: {
    height: '50%',
    width: '50%',
    alignSelf: 'center',
  },
});
